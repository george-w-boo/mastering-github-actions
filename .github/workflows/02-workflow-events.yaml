name: 02 - Workflow Events
on:
  pull_request:
  schedule:
    - cron: "* * * * *"
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo ${{ github.event_name }}
